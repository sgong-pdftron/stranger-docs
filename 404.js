import React from 'react';

export default () => (
  <div>
    <h1>Page Not Found (404)</h1>
    <div>Seems like you were trying to get a page that doesn't exist. Visit our hompeage.</div>
  </div>
);